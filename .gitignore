# macOS
.DS_Store

# build scratch (raw render; the og-image.html source is kept for reproducibility)
_build/og-raw.png

# editor
.vscode/
*.swp

# Visiting card working files (not part of the site)
_cards/

# LinkedIn / social artwork (not part of the site)
_social/
